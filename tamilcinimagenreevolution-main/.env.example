# Example environment variables (do NOT commit real keys)
# Copy this file to .env and fill values before running scripts that use them.
# Use angle-bracket placeholders so automated scanning tools ignore them.
OMDB_API_KEY=<YOUR_OMDB_KEY_HERE>
